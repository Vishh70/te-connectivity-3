name: "V5 Production Verification"

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  verify-backend:
    name: "Backend - Smoke Tests"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CI Test Dependencies
        run: |
          python -m pip install --upgrade pip
          # Install only the packages needed for API smoke tests in CI
          # (avoids heavy ML dependencies and Windows-only packages)
          pip install fastapi[all] httpx pytest pyjwt pandas joblib lightgbm scikit-learn shap anyio

      - name: Run Backend Smoke Tests (Pytest)
        env:
          CI: "true"
          LOCAL_DEV: "true"
        run: |
          pytest tests/test_api.py -v --no-header --tb=short

  verify-frontend:
    name: "Frontend - Build Check"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm ci

      - name: Build Frontend (Vite)
        run: |
          cd frontend
          npm run build
